<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewPlace"></activity>
        <activity android:name=".khulnapart1" />
        <activity android:name=".barishalpart1" />
        <activity android:name=".chittagangpart1" />
        <activity android:name=".dhakapart1" />
        <activity android:name=".bandarban" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".creditActivity" />
        <activity android:name=".helpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>